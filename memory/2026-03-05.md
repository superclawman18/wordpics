# Memory Log — 2026-03-05

- Captured plan to port the single-page Lode Runner experience from `apps/loderunner/loderunner.html` into `apps/next-loderunner`, including actor loop, digging, holes, enemies, and UI, while replacing the simplistic level generator with a more structured templating approach so the Next.js version loads playable maps via `levels.json`.
- Added `scripts/nightly-automation.js` plus `reports/nightly-report-2026-03-05T19-22-30.md` so nightly runs lint + level generation and reports on gold/ladder coverage; the helper now flags any level missing gold, climb paths, or enemies.
- Captured the workflow and the cron suggestion inside `docs/nightly-automation.md` so we know how to trigger the automation and read tomorrow's log.
- 2026-03-05 晚间：确认了夜间自动化流程（lint + 关卡生成 + 报告）并在 docs/nightly-automation.md 记录操作方式；脚本现已能指出缺金币/爬梯/敌人关卡；准备把这些要点当作每日总结素材
- 2026-03-05 晚间新增要求：每天晚上整理当天对话关键点写入 memory 文件，确保第二天早上可以复盘。
- Added `scripts/daily-backup.sh` to copy AGENTS/SOUL/IDENTITY/USER/TOOLS/HEARTBEAT daily into `backups/<date>` and ran it for 2026-03-05.
- Nightly automation now runs `./scripts/daily-backup.sh` before lint/generation, so each report (latest at reports/nightly-report-2026-03-05T19-35-37.md) records the backup status as well. EOF
- Nightly automation now runs `./scripts/daily-backup.sh` before lint/generation, so each report (latest at reports/nightly-report-2026-03-05T19-35-37.md) records the backup status as well.
- HEARTBEAT.md now documents the nightly automation command (`node scripts/nightly-automation.js`) so the heartbeat/cron check explicitly runs the backup+lint+generator pipeline every evening at 23:00 GMT.
- HEARTBEAT.md now documents the nightly automation command (`node scripts/nightly-automation.js`) so the heartbeat/cron check explicitly runs the backup+lint+generator pipeline every evening at 23:00 GMT.
